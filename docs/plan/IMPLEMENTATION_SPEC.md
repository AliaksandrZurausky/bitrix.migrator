# Bitrix Migrator — спецификация реализации (Cloud → Box)

Документ фиксирует требования и договорённости по модулю миграции из облачного Bitrix24 в коробочный Bitrix.
Цель — чтобы реализация не «плыла» при смене контекста/беседы.

---

## 1) Цель и тип задачи

### 1.1. Цель
Разовая миграция данных из облачного Bitrix24 в коробочный Bitrix **без удаления данных в облаке**:
- **Структура компании (отделы)**
- **Пользователи** (включая создание «уволенных» заглушек при необходимости)
- **Рабочие группы / Проекты (Workgroups)**
- **Диск (Disk)**: перенос **структуры + файлов**, без переноса прав доступа/шаринга
- CRM: компании/контакты/лиды/сделки
- С сохранением всех связей
- С максимально возможным переносом таймлайна
- С переносом связанных задач и файлов (если они встречаются в таймлайне/задачах/карточках)

### 1.2. Это НЕ синхронизация
Не делаем периодическую синхронизацию "туда-сюда".
Миграция может запускаться повторно только для продолжения после паузы/сбоя или повторного прогона без дублей.

---

## 2) Источник и авторизация

- **Источник данных:** REST API Bitrix24 (облако; потенциально коробка-источник через REST, если потребуется).
- **Формат webhook URL:** `https://<portal>/rest/<user>/<token>/`
- **Метод вызова:** `<webhook>/<method>.json`, параметры — JSON body

**Хранение webhook:**
- Можно хранить в Option (доступ только из админки).
- UI: ввод строки webhook.

---

## 3) Базовые принципы миграции

### 3.1. Асинхронность
Запуск по кнопке из админки, но выполнение **не привязано к сессии браузера**.
Исполнитель: **агент (CAgent)**.

### 3.2. Управление
Нужно управление:
- **Start** — запустить миграцию
- **Pause** — graceful stop: завершить текущий шаг и выйти
- **Resume** — продолжить с checkpoint

### 3.3. Идемпотентность по cloud_id
**Ключевое правило:** повторный запуск НЕ создаёт дублей, если элемент уже переносился мигратором.

Для этого:
- В сущностях коробки храним `UF_CLOUD_ID` и `UF_CLOUD_URL`
- И/или ведём MigratorMap (cloud_id → local_id)

---

## 4) Политики конфликтов

### 4.1. Предположение "чистая система"
Считаем, что коробка чистая.
Если в коробке встретили сущность **без** `UF_CLOUD_ID`, то:
- не трогаем (skip)
- пишем в лог (как "unexpected existing data")

### 4.2. Повторный прогон "уже переносилось мигратором"
Если найден локальный объект с заполненным `UF_CLOUD_ID`, то это НЕ "конфликт", а **повторный прогон**.

**Перед запуском миграции** задаётся политика:
- **SKIP** (дефолт) — пропустить
- **UPDATE** — обновить поля по данным из облака
- **DELETE_AND_RECREATE** — удалить и создать заново
- **DUPLICATE** — создать дубль

Политика сохраняется в MigratorState.

---

## 5) MVP сущности и порядок (pipeline)

Фазы (с checkpoint и возможностью pause между шагами):

1. **Dry run** (без записи данных в бизнес-сущности; запись результата отчёта допустима)
2. **Подготовка коробки:**
   - создание системных UF-полей `UF_CLOUD_ID`/`UF_CLOUD_URL` (если нет)
   - создание воронок/стадий сделок по выбранному плану
   - создание/подготовка пользовательских полей (как в облаке) по выбранному плану
3. **Перенос структуры (отделы)**
4. **Перенос пользователей** (маппинг + создание заглушек)
5. **Перенос рабочих групп / проектов (Workgroups)**
6. **Перенос Диска (Disk)** (в выбранном режиме, структура+файлы без прав)
7. **Перенос базовых CRM сущностей** (в правильном порядке):
   - companies
   - contacts (с привязкой к companies)
   - leads
   - deals (с привязками)
8. **Перенос задач** (после workgroups и CRM)
9. **Перенос таймлайна** (максимально возможный перенос; что нельзя — логируем)
10. **Перенос/прикрепление файлов-вложений** (если включено отдельной фазой)
11. **Финализация:**
   - отчёт
   - статистика
   - список проблем (пользователи/файлы/ошибки)

---

## 6) Dry run — что обязан сделать

**Dry run обязателен.**

Задачи dry run:
- Проверка webhook (доступ/права)
- Подсчёт объёмов по сущностям (departments/users/workgroups/disk + CRM)
- Список пользовательских полей по каждой CRM сущности (`crm.*.fields`)
- Список воронок и стадий сделок (для выбора, что переносим)
- Оценка файлов (кол-во, примерные объёмы если возможно)
- Отчёт по пользователям:
  - сколько матчится однозначно
  - сколько не найдено/неоднозначно (будут созданы "уволенные" заглушки)

### 6.1. UI после Dry run (План миграции)
После dry run пользователь видит **план**, где **по умолчанию включено всё**, но можно выключать:
- Структура / Пользователи / Рабочие группы
- Диск: `FULL_DISK` / `ATTACHMENTS_ONLY` / `NONE`
- Сущности (companies/contacts/leads/deals)
- Воронки и стадии сделок (чекбоксы)
- Пользовательские поля (чекбоксы "не переносить")
- Таймлайн: типы событий (если получится классифицировать)
- Задачи: комментарии/чек-листы/файлы/…
- Файлы: качать/загружать + (опционально) ограничения

Состояние плана хранить в **MigratorState** (`UF_SETTINGS_JSON`).

---

## 7) Пользователи (маппинг и заглушки)

### 7.1. Маппинг пользователей
**Правило:**
1. Если у cloud-пользователя есть email → ищем в коробке по email (точно)
2. Если по email не нашли/пустой → используем cloud `user.search` по ФИО (строка "Имя Фамилия") для получения кандидатов
3. Матчим среди **всех** пользователей коробки (включая неактивных/уволенных)
4. Если найден **1 кандидат** → используем его
5. Если найдено **0 или 2+** → создаём "уволенного-заглушку"

### 7.2. Уволенный пользователь-заглушка
Создание через `CUser::Add`:
- `ACTIVE = 'N'`
- `LOGIN`/`EMAIL` берём из облака
- если email недоступен/пустой → генерируем уникальный email по нумератору: `migrator_ghost_<n>@invalid.local`
- **не отправлять** email-уведомления о регистрации
- **группы:** как у обычного сотрудника (дефолтные группы берём из настроек мигратора, не хардкодим)
- **отдел** назначаем по маппингу отделов (если переносим отделы) или в дефолтный

**Идея использования заглушки:**
Все "проблемные" элементы будут назначены на неё → легко фильтровать и переразносить.

---

## 8) Воронки и стадии сделок

**Требование:**
- Создать в коробке те же воронки и стадии, что в облаке, но **только те, что выбраны** в плане после dry run
- Методы: `crm.category.add`, `crm.status.add`

После создания:
- При миграции сделок мапить `STAGE_ID` на созданные стадии

---

## 9) Пользовательские поля CRM

**Требование:**
- **"Очень важно"**: создать в коробке те же поля, что в облаке, для:
  - сделок/лидов/контактов/компаний
- После dry run должна быть возможность **исключить часть полей чекбоксами**

**Минимально обязательно:**
- Системные поля `UF_CLOUD_ID` и `UF_CLOUD_URL` (для каждой сущности)

**Нюанс:**
- Типы полей в облаке могут быть сложными (списки, привязки, множественные)
- Пока что: переносим простые типы (`string`, `integer`, `date`)
- Для списков (`enumeration`) — создаём структуру, значения мапим вручную или по тексту
- Для привязок (`crm_entity`) — мапим через `MigratorMap` (cloud_id → local_id)

**Важно для привязок:**
Если поле "привязка к компании" в сделке содержит `cloudCompanyId` → ищем `localCompanyId` по `UF_CLOUD_ID`/`Map` и ставим local ID.

---

## 10) Таймлайн

**Требование:**
- Переносим "максимально возможный перенос таймлайна"
- Всё что реально перенести через API — переносим
- Что не поддерживается API — логируем как "не перенесено"

**Приоритет для MVP:**
- **Комментарии** — обязательно (`crm.timeline.comment.add`)
- Звонки/письма/активности — если API позволяет
- Системные события (смена стадии, изменение ответственного) — если API позволяет

---

## 11) Задачи

**Требование:**
- Переносим **все задачи, связанные с CRM сущностями** (`UF_CRM_TASK`)
- Переносим после Workgroups и CRM (чтобы все привязки были готовы)
- Переносим "всё что можно": поля, комментарии, чек-листы, файлы, участники, статусы

**Файлы в задачах:**
- Сначала загружаем на Диск в папку `Migrator/Tasks/<cloudTaskId>/`
- Потом прикрепляем к задаче по ID файлов диска

---

## 12) Диск и файлы

### 12.1. Режимы переноса
- `NONE` — не переносим диск и файлы
- `ATTACHMENTS_ONLY` — переносим только файлы-вложения из таймлайна/задач/карточек
- `FULL_DISK` — переносим весь диск: **структура папок + файлы**, без прав/шаринга

### 12.2. Стратегия
1. Все файлы сначала **выгружаются в папку на Общем диске** (одна корневая папка миграции)
2. Для вложений используем структуру `Migrator/<Entity>/<cloudId>/...`
3. Для FULL_DISK создаём структуру `Migrator/Disk/...` с повторением дерева папок
4. При загрузке использовать `generateUniqueName=true`
5. **Копии файлов допустимы** (дедупликацию можно сделать позже)

**Методы:**
- Загрузка на Диск: `disk.folder.uploadfile`

---

## 13) Хранилище данных (HL-блоки)

Создаём минимум **4 HL-блока**:

### 13.1. MigratorState (состояние миграции)
**Назначение:** хранить настройки подключения (кроме секрета), выбранные чекбоксы плана, текущую фазу, прогресс, флаг pause, режим конфликтов и т.п.

**Пример полей:**
- `UF_STATUS`: NEW / DRY_RUN_DONE / RUNNING / PAUSED / FINISHED / ERROR
- `UF_CURRENT_PHASE`: string
- `UF_PAUSE_FLAG`: boolean
- `UF_CONFLICT_POLICY`: SKIP / UPDATE / DELETE_AND_RECREATE / DUPLICATE
- `UF_SETTINGS_JSON`: string (или text) — сериализованный план/настройки
- `UF_LAST_ERROR`: text
- `UF_STARTED_AT`, `UF_UPDATED_AT`

### 13.2. MigratorQueue (очередь работ)
**Назначение:** разложить миграцию на атомарные задачи, чтобы агент мог шагать, продолжать после падений, и было видно прогресс.

**Пример полей:**
- `UF_ENTITY_TYPE`: department/user/workgroup/disk_folder/disk_file/deal/lead/contact/company/task/file/...
- `UF_CLOUD_ID`: int/string
- `UF_LOCAL_ID`: int (если уже создан)
- `UF_STATUS`: NEW / IN_PROGRESS / DONE / ERROR / SKIPPED
- `UF_RETRIES`: int (счётчик попыток)
- `UF_LAST_ERROR`: text
- `UF_PRIORITY`: int
- `UF_DEPENDS_ON`: string (опционально: зависимости)
- `UF_UPDATED_AT`

### 13.3. MigratorMap (маппинг идентификаторов)
**Назначение:** хранить соответствие cloud → local по сущностям, пользователям, файлам и т.д.

**Пример полей:**
- `UF_ENTITY_TYPE`: department/user/workgroup/disk_folder/disk_file/company/contact/deal/lead/task/file
- `UF_CLOUD_ID`: string/int
- `UF_LOCAL_ID`: int
- `UF_CLOUD_URL`: string (опционально)
- `UF_META_JSON`: text (опционально)

### 13.4. MigratorLog (журнал)
**Назначение:** понятные логи для админа и разработчика.

**Пример полей:**
- `UF_LEVEL`: INFO / WARN / ERROR / DEBUG
- `UF_MESSAGE`: text
- `UF_CONTEXT`: string (phase/entity/id)
- `UF_PAYLOAD`: text/json (безопасно, без секретов)
- `UF_CREATED_AT`

**Требования к логам:**
- Уровни: должна быть возможность выбора (все/info/warn/error/debug)
- Логи должны быть понятными админу И разработчику
- **НЕ хранить** raw request/response к API (риск по безопасности/PII)

---

## 14) Агент (исполнитель)

**Требование:**
- Исполнитель: **агент (CAgent)**
- Агент берёт пачку задач из Queue (например, 50 задач за запуск или 30 секунд работы — что наступит раньше)
- Обрабатывает шаг
- Фиксирует прогресс в State/Queue/Map
- Проверяет флаг **PAUSE** → завершает текущий шаг и выходит

**Ретраи:**
- **3 попытки** на задачу
- После 3 попыток → статус `FAILED`, продолжаем миграцию
- В конце формируем отчёт "Failed tasks: 12"

---

## 15) UI админки (одна страница с табами)

**Требование:**
- Одна страница модуля в стиле админки Битрикса
- **Нужен пункт в меню** в админке (не только "Настройки модуля")
- **В стилистике Битрикса:** чтобы не выделялось на общем фоне

**Табы:**
1. **Подключение** (Connection)
   - Ввод webhook
   - Проверка подключения
   - Кнопка "Dry run"

2. **Dry run** (отчёт)
   - Результаты dry run (кол-во сущностей, воронки, поля, файлы, пользователи, workgroups, disk)
   - Кнопка "Сформировать план миграции"

3. **План миграции** (Plan)
   - Чекбоксы "что переносим" (структура/пользователи/workgroups/disk/сущности/воронки/поля/таймлайн/задачи/файлы)
   - Disk mode: `NONE` / `ATTACHMENTS_ONLY` / `FULL_DISK`
   - Выбор политики конфликтов (SKIP/UPDATE/DELETE_AND_RECREATE/DUPLICATE)
   - Кнопка "Старт миграции"

4. **Миграция** (Migration)
   - Кнопки: Start/Pause/Resume
   - Прогресс-бар (из Queue)
   - Счётчики по типам сущностей (processed/total)
   - Текущая фаза

5. **Логи** (Logs)
   - Фильтры уровней (INFO/WARN/ERROR/DEBUG)
   - Фильтр по типу сущности
   - Экспорт логов
   - Кнопка "Очистить логи"

**Права доступа:**
- Только **администраторы** (проверка `$USER->IsAdmin()`)
- Дополнительных уровней прав не нужно

---

## 16) Критичные исправления (из текущего кода)

### 16.1. HTTP 429 (Rate Limit)
**Проблема:** CloudAPI не обрабатывает HTTP 429 (превышение лимитов).

**Решение:**
- Ловить HTTP 429 в `CloudAPI::request()`
- Читать заголовок `Retry-After` (обычно 60 секунд)
- Делать `sleep()` и повторять запрос (1 попытка)
- Логировать в MigratorLog

### 16.2. Утечка памяти в fetchAll()
**Проблема:** `CloudAPI::fetchAll()` пытается загрузить ВСЕ страницы в память → утечка памяти при больших объёмах.

**Решение:**
- Изменить `fetchAll()` на возврат только **1 страницы** (или переименовать в `fetchPage()`)
- Для миграции использовать **пагинацию** через Queue: каждая задача = одна страница

### 16.3. SSL verify
**Проблема:** отключение `CURLOPT_SSL_VERIFYPEER/VERIFYHOST` в проде недопустимо.

**Решение:**
- Включить проверку SSL, корректно обрабатывать ошибки TLS и логировать.

### 16.4. UF_CLOUD_ID поля
**Проблема:** В текущем коде не создаются поля `UF_CLOUD_ID` и `UF_CLOUD_URL` при установке модуля.

**Решение:**
- В `install/index.php` при установке создавать поля для всех CRM сущностей:
  - `UF_CRM_COMPANY_CLOUD_ID` (integer)
  - `UF_CRM_COMPANY_CLOUD_URL` (string)
  - Аналогично для Contact, Deal, Lead
- При удалении модуля — удалять эти поля

---

## 17) Открытые вопросы (на реализацию)

### 17.1. Таймлайн: типы событий
- Полный перечень типов событий таймлайна, которые реально можно перенести в коробку 1:1
- Возможно, часть будет через комментарии/активности
- Требуется проверка доступности методов API

### 17.2. Задачи: полнота переноса
- Полный перечень того, что "можно перенести по задачам" (комментарии/чек-листы/файлы/участники/статусы)
- Определить по доступности методов API в текущем облаке

### 17.3. Пользовательские поля: сложные типы
- Как переносить поля типа "список" (`enumeration`) — создавать значения или мапить по тексту?
- Как переносить поля типа "привязка к CRM" — нужна правильная последовательность миграции

### 17.4. FULL_DISK: покрытие API
- Подтвердить перечень методов Disk API для обхода дерева папок и скачивания/загрузки файлов.

---

## 18) Блочный план реализации

### Блок 1: Критичные исправления 🔴
- HTTP 429 + утечка памяти в CloudAPI + SSL verify
- UF_CLOUD_ID поля при установке

### Блок 2: DryRunService 🔴
- Dry run должен включать departments/users/workgroups/disk + CRM оценки

### Блок 3: MigrationService::prepareQueue() 🔴
- Формирование очереди по плану миграции
- Приоритеты задач
- DEPENDS_ON для иерархий (departments/workgroups/disk)

### Блок 4: DepartmentsMigrator 🔴

### Блок 5: UserMapService / UsersMigrator 🔴

### Блок 6: WorkgroupMigrator 🔴

### Блок 7: DiskMigrator 🔴
- `FULL_DISK` (структура+файлы) и `ATTACHMENTS_ONLY`

### Блок 8: CRM Migrators 🔴
- Company/Contact/Lead/Deal

### Блок 9: MigratorAgent::processTask() 🔴
- Реализация обработки задач в агенте

### Блок 10: UI Migration tab 🔴
- Start/Pause/Resume + прогресс

---

## 19) Требования к коммитам

**ВАЖНО:**
- **НИКОГДА не пушить без явного одобрения** "Ок, пуши"
- Только то, что обговорили
- Хочу рефакторить? Сначала спрашиваю
- Несколько файлов? Один коммит

---

## Заключение

Этот документ — **единая точка правды** для реализации модуля миграции.
При смене контекста/беседы — обращаться сюда за требованиями.

Все изменения требований фиксировать здесь же.
